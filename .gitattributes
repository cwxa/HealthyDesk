# 跨平台行尾约定
#
# 背景：本仓库要在 Windows / macOS / Linux 三处构建。Windows 上检出时若把 shell 脚本
# 变成 CRLF，Linux/macOS 上会直接报 `bad interpreter: No such file or directory`；
# 这类问题在 Windows 本地永远复现不出来，只会炸在 CI 里。
#
# 约定：仓库内一律存 LF，只有 Windows 专用脚本例外。

# ---- 必须是 LF：Unix 可执行脚本 / 构建配置 ----
*.sh      text eol=lf
gradlew   text eol=lf
Podfile   text eol=lf

# ---- 必须是 CRLF：Windows 专用脚本 ----
*.bat     text eol=crlf
*.cmd     text eol=crlf
*.ps1     text eol=crlf

# ---- 明确为二进制：不要做任何行尾或编码转换 ----
*.png       binary
*.jpg       binary
*.jpeg      binary
*.gif       binary
*.ico       binary
*.icns      binary
*.task      binary
*.jar       binary
*.jks       binary
*.keystore  binary
*.ttf       binary
*.woff      binary
*.woff2     binary
*.zip       binary
*.dmg       binary
*.exe       binary

# ---- 工程文件：用 LF 存，避免 Xcode / Git 反复改写 ----
*.pbxproj text eol=lf
*.plist   text eol=lf

# ---- 配置：统一 LF ----
# 漏了这条时，Windows 上 add 会报 "LF will be replaced by CRLF"（无害但是噪音）；
# 更要紧的是 YAML 解析器对行尾不敏感，出问题也不报错，不如一开始就钉死。
*.yml     text eol=lf
*.yaml    text eol=lf

# ---- 文档与忽略规则：统一 LF ----
# 同理，仓库里存 LF，别让 autocrlf=true 在检出时改写成 CRLF（那会让 `git diff`
# 在只有行尾差异时也报改动，掩盖真正的修改）。
*.md         text eol=lf
.gitignore   text eol=lf

# ---- 源码与结构化配置：统一 LF ----
# 🔴 源码这条是**守卫正确性**的要求，不只是审美：
#    本机 `core.autocrlf=true` 检出 CRLF、CI 检出 LF。任何「按行处理源码」的守卫
#    （剥 `#` 注释、匹配行尾）都会因此两端行为不同 —— 因为 `.` **不匹配 `\r`**、
#    非 multiline 的 `$` 只匹配串尾，`/\s*#.*$/` 在 CRLF 行上会**静默不匹配**
#    → 注释里提到旧写法就被误判成违规，变成「本机红、CI 绿」这种最难查的分叉。
#    （v1.6.1 实测踩到；同时守卫侧也做了 CRLF 归一，两头都堵。）
*.py      text eol=lf
*.mjs     text eol=lf
*.js      text eol=lf
*.ts      text eol=lf
*.tsx     text eol=lf
*.json    text eol=lf
.gitattributes text eol=lf
