node_modules/
# dist/  # Temporarily disabled for electron-builder
dist-electron/
release/
release2/
backend/__pycache__/
backend/**/__pycache__/
backend/data/
# 🔴 必须写成根锚定的 `/data/`：裸 `data/` 会在**任意深度**匹配，把 `src/data/` 一起忽略掉。
#    本轮实测踩到 —— 新建的 `src/data/exercises.ts`（动作库，S7）在 `git status` 里**完全不出现**，
#    本机跑守卫全绿（文件在磁盘上），CI 却因为仓库里没有这个文件而红。
#    运行时数据库在仓库根的 `data/`（见 backend/config.py:DB_PATH），按根锚定才是本意。
/data/
build/
*.pyc
*.pyo
*.spec
.env
.vite/
.DS_Store
Thumbs.db
/dist
# 沙箱的批量删除配额耗尽后，「改名代替删除」会留下 dist.old-<tag>/（见 docs/DEVELOPMENT.md §8.3）
/dist.old-*/
*.log
.workbuddy/
.buildenv/

# 🔴 安卓签名材料：绝不可入库（keystore.properties 里是**明文**口令）。
# 兜底规则：即使有人在别处新建同名文件也不会被 `git add -A` 带走。
# （android/.gitignore 里另有一份等价规则，两处都留着是刻意的冗余。）
*keystore.properties*
*.jks
*.keystore
